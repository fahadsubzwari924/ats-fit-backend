/**
 * Contact fields expected by the Handlebars `contactLine` helper when
 * rendering resume PDF/HTML templates.
 */
export interface ContactLineFields {
  email?: string;
  phone?: string;
  location?: string;
  linkedin?: string;
  github?: string;
}
